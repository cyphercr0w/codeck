# Full Codebase Audit — 2026-03-17

## Scope

4 parallel agents analyzed the entire Codeck codebase:
- **Backend Runtime** (26 issues): services, routes, WebSockets, PTY, memory
- **Frontend Web** (28 issues): components, signals, terminal, UX, state
- **Daemon + CLI** (26 issues): process management, Docker, auth, LAN
- **Infrastructure** (36 issues): Docker, tests, deps, docs, structure

Total raw issues: ~116. After deduplication and noise filtering: **62 actionable items**.

---

## CRITICAL (11)

| # | Area | Issue | File(s) |
|---|------|-------|---------|
| C1 | Backend | Race condition in PTY session output broadcast — iterating `sessionClients` while it can be modified by close handler | `websocket.ts:343-354` |
| C2 | Backend | Unvalidated Docker CLI output in port manager — parsed path used in `docker run -v` mount | `port-manager.ts:79-88` |
| C3 | Backend | Session capture stream error silently drops all future writes — no retry, no user notification | `session-writer.ts:96-99` |
| C4 | Backend | Memory write lock is not atomic — two async writers can both pass the check before either acquires | `memory.ts:49-56` |
| C5 | Daemon | Static `CODECK_INTERNAL_SECRET` in WS URL query param — never rotates, visible in logs | `ws-proxy.ts:119-121` |
| C6 | Daemon | Port manager mutates state before container restart — if restart fails, state is inconsistent | `port-manager.ts:51-69` |
| C7 | Daemon | Secret mismatch between daemon/runtime silently breaks WS — no detection in `handleWsUpgrade()` | `ws-proxy.ts` vs `proxy.ts:53-62` |
| C8 | CLI | Shell injection in LAN command — PowerShell string interpolation with config-sourced values | `lan.ts:86-94` |
| C9 | CLI | Unvalidated Docker Compose override path — symlink traversal to arbitrary file write | `compose.ts:75-78` |
| C10 | Frontend | Race condition: terminal mount deferred to rAF but WS output can arrive before `attachSession()` | `ClaudeSection.tsx:146-148` |
| C11 | Frontend | Stale closure in WS reattach handler — iterates `sessions.value` captured at open time | `ws.ts:185-191` |

---

## HIGH (19)

| # | Area | Issue | File(s) |
|---|------|-------|---------|
| H1 | Backend | No rate limiting on `/upload-image` — disk exhaustion DoS | `files.routes.ts:270-299` |
| H2 | Backend | Synchronous `isClaudeAuthenticated()` disk read races with CLI writes | `console.routes.ts:20-22` |
| H3 | Backend | Path injection in session detection — `encodeProjectPath` not validated to stay within `projectsDir` | `console.ts:68-69` |
| H4 | Backend | Missing null check in OAuth account info merge | `auth-anthropic.ts:65` |
| H5 | Backend | Orphaned polling interval if session destroyed during `detectConversationId()` | `console.ts:93-138` |
| H6 | Backend | WeakMap token buckets never cleaned for idle WS connections | `websocket.ts:50-71` |
| H7 | Daemon | Unbounded `activeSessions` map — no max limit, lazy cleanup only | `auth.ts:76-122` |
| H8 | Daemon | Rate limiter lockout duration integer overflow via env var | `rate-limit.ts:85-86` |
| H9 | Daemon | Docker Compose project name derived from basename only — collisions between users | `init.ts:17-22` |
| H10 | Daemon | Process cleanup in CLI `start` has no timeout — can hang forever | `start.ts:148-188` |
| H11 | Daemon | Missing Content-Length validation in proxy forwarding | `proxy.ts:98-108` |
| H12 | Daemon | No port number validation before writing override YAML | `compose.ts:9-29` |
| H13 | Daemon | `timingSafeEqual` not used for token comparison in `validateSession()` | `auth.ts:202` |
| H14 | Frontend | Silent poll failure in LoginModal — errors swallowed indefinitely | `LoginModal.tsx:155` |
| H15 | Frontend | Event listener leak — blur handler on xterm textarea not removed in `destroyTerminal()` | `terminal.ts:127-131` |
| H16 | Frontend | `sessionStatus` signal initialized empty — sessions not tracked until explicit status update | `store.ts:83` |
| H17 | Frontend | Unhandled 401 in `apiFetch` — some callers ignore the thrown error, leaving UI inconsistent | `api.ts:38-44` |
| H18 | Frontend | `activeSessionId` missing from dependency array in image paste effect — stale closure | `ClaudeSection.tsx:182-188` |
| H19 | Infra | 6 npm audit vulnerabilities (esbuild DoS, rollup path traversal CVE-2025-45582, qs bypass, flatted DoS) | `package.json` |

---

## MEDIUM (22)

| # | Area | Issue |
|---|------|-------|
| M1 | Backend | No timeout on `ptySpawn()` — hangs indefinitely on bad state |
| M2 | Backend | Output buffer naive ring buffer — bursty output causes data loss during disconnects |
| M3 | Backend | Docker socket mount path may not exist inside container |
| M4 | Backend | Shell session `cwd` not validated to stay within WORKSPACE |
| M5 | Backend | Silent failure in session restoration — no UI notification |
| M6 | Backend | `stat()` on symlinks in directory listing can hang |
| M7 | Backend | Terminal resize allows cols=1/rows=1 — broken rendering |
| M8 | Daemon | Audit log truncated at 200 entries silently |
| M9 | Daemon | CLI config lock file left behind on crash — blocks all commands |
| M10 | Daemon | WS ping failures silently ignored — stale connections linger 75s |
| M11 | Daemon | No timeout on `docker compose down` in restart command |
| M12 | Daemon | Audit buffer lost on daemon crash — up to 20 entries |
| M13 | Daemon | Session expiry check without lock — race on concurrent requests |
| M14 | Frontend | Terminal tabs no overflow handling on mobile — layout break |
| M15 | Frontend | Loading session spinner stuck if API error after UI render |
| M16 | Frontend | Resize timer fires after `destroyTerminal()` — unnecessary work |
| M17 | Frontend | Aggressive 1.5s login poll accumulates on fast close/reopen |
| M18 | Infra | No healthcheck in isolated compose mode |
| M19 | Infra | 122 sync fs operations blocking event loop across services |
| M20 | Infra | Auth tests: 9 failures from race conditions and timing assumptions |
| M21 | Infra | Vite 3 major versions behind, express 1 major behind |
| M22 | Infra | God modules: `auth-anthropic.ts` 1162 lines, `proactive-agents.ts` 1095, `git.ts` 823 |

---

## LOW (10 — summarized)

- No error boundaries per section (only top-level)
- localStorage access without try-catch in some spots
- Missing focus management after modal close
- Dead code: `MemorySection.tsx` (692 lines, unreferenced)
- CSS `@keyframes spin` duplicated across files
- No CI/CD (GitHub Actions)
- No `.env.example` template
- pnpm installed in Docker image but unused
- No `engines` field in root package.json
- Incomplete turbo.json (missing lint, type-check, format tasks)

---

## Recommended Action Order

### Phase 1 — Security & Stability (do now)
1. Fix shell injection in LAN command (C8)
2. Fix path traversal in compose override (C9)
3. Fix Docker CLI output validation in port manager (C2)
4. Fix port manager state-before-restart race (C6)
5. Fix memory write lock atomicity (C4)
6. Run `npm audit fix` (H19)
7. Add rate limiting to `/upload-image` (H1)

### Phase 2 — Reliability (this week)
8. Fix terminal attach race condition (C10)
9. Fix WS reattach stale closure (C11)
10. Fix session broadcast race (C1)
11. Fix session capture silent failure (C3)
12. Add `timingSafeEqual` to token validation (H13)
13. Fix orphaned polling intervals (H5)
14. Add process cleanup timeout in CLI start (H10)
15. Fix LoginModal silent poll failure (H14)

### Phase 3 — Code Quality (next 2 weeks)
16. Split god modules (M22)
17. Convert critical sync fs to async (M19)
18. Fix auth test failures (M20)
19. Delete dead MemorySection.tsx
20. Add healthcheck to isolated compose (M18)
21. Update KNOWN-ISSUES.md (remove fixed items)

### Phase 4 — Infrastructure (month)
22. Add GitHub Actions CI/CD
23. Incremental dependency upgrades (Vite 5→8, Express 4→5)
24. Rotate WS internal secret (C5) — needs design decision
25. Add E2E test suite
26. Implement async-mutex for session state
